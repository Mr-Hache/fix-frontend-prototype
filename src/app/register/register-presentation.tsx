import Image from 'next/image';

const registerPresentation = () => {
  return (
    <div className="flex items-center justify-center overflow-hidden flex-col w-screen h-dvh "></div>
  );
};

export default registerPresentation;
